"""Shared library used by both the Flask web app (web/) and the standalone
data-processing programs (standalone/) — the one canonical place for
Database, Conversion, CONST, regional host mappings, and state standards.
"""
